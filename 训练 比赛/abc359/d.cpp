#include <bits/stdc++.h>

using namespace std;

constexpr int N = 12;
constexpr int M = (1 << 10) + 10;

int n, k;
string s;

int f[N][M];

signed main() {
    cin >> n >> k >> s;
    s = "#" + s;
    return 0;
}